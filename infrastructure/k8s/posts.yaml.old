apiVersion: v1
kind: Pod
metadata:
  name: postspod
spec:
  containers:
  - name: posts-service
    image: amrmonier/posts-service:0.0.2
    resources:
      limits:
        memory: "128Mi"
        cpu: "500m"

